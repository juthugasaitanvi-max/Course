// // let qwerty={
// // name:"Saitanvi",
// // school:{
// // name:"T.I.M.E",
// // class:{
// //     grade:7,
// //     section:"E",
// // },
// // },
// // phone:1234567890
// // }

// // console.log(qwerty.school.name) 
// // console.log(qwerty.school.class.section)

// // Math Objects :  They are built-in objects in JavaScript. Example: Math.round(),Math.sqrt(),Math.random.
// // Example:
// // console.log(Math.round(4.7))
// // console.log(Math.sqrt(144))
// // console.log(Math.random() )

// // Arrays: Array is a data structure in which we can store more than one value at a time.
// // .pop()- removes the last element of the array.
// // .push()- adds an element at the end of the array.
// // .length tells you the count of the number of elements of the array.

// // let fruits=['apple','banana','mango','guava']
// // console.log(fruits.pop())
// // console.log(fruits)
// // console.log(fruits.push("mango"))
// // console.log(fruits)
// // console.log(fruits.length)
// // console.log(fruits[0])
// // console.log(fruits[3])
// // console.log(fruits[4])

// // Call Stack: Its used in order to keep the track of multiple function calls. It follows a principle known as LIFO (Last in First Out).

// // Example:

// // function makeBreakfast(){
// //     cookEggs()
// //     console.log("Breakfast Ready.")
// // }

// // function cookEggs(){
// //     console.log("Cooking Eggs.")
// // }

// // makeBreakfast()

// // Activity 2: 
// // <!--
// // 1) Start the HTML document with `<!DOCTYPE html>` and open `<html>`.

// // 2) Inside the `<body>`:

// // 3) Add headings:
// //    a) `<h2>` with text: "JavaScript Array Methods"
// //    b) `<h2>` with text: "join() and pop()"

// // 4) Add a paragraph explaining the methods:
// //    a) join() converts array elements into a single string.
// //    b) pop() removes the last element from an array.

// // 5) Create three empty paragraphs to display outputs:
// //    a) `<p id="myjoin"></p>` for join() output
// //    b) `<p id="myfirstpop"></p>` for the array before pop()
// //    c) `<p id="mysecondpop"></p>` for the array after pop()

// // 6) Add a `<script>` tag to write JavaScript.

// // 7) Inside the `<script>` tag:
// //    a) Create an array named `fruits` with values: Banana, Orange, Apple, Mango.
// //    b) Use `fruits.join(" * ")` and display it inside `"myjoin"`.
// //    c) Display the original array inside `"myfirstpop"`.
// //    d) Use `fruits.pop()` to remove the last element.
// //    e) Display the updated array inside `"mysecondpop"`.

// // 8) Close all tags properly.
// // -->

// Activity 3:                                                                                                            
// //<!--
// 1) Start the HTML document with `<!DOCTYPE html>` and open `<html>`.

// 2) Inside the `<body>`:
//    a) Add a heading `<h2>` with text: "JavaScript Functions".
//    b) Add a paragraph describing the task:
//       "Do a calculation and then display the result."
//    c) Add an empty paragraph `<p id="demo"></p>` to show the output.

// 3) Add a `<script>` tag to write JavaScript.

// 4) Create a function named `add(a, b)`:
//    a) Return the sum of `a + b`.

// 5) Create a function named `average(a, b)`:
//    a) Call `add(a, b)` to get the sum.
//    b) Divide the sum by `2` and return the average.

// 6) Call the `average()` function:
//    a) Store the returned value in a variable named `result`.
//    b) Example call: `average(10, 20)`.

// 7) Display the final result on the webpage:
//    a) Use `document.getElementById("demo").innerHTML = result;`

// 8) Close all tags properly.
// -->





